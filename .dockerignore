.git
node_modules
**/node_modules
dist
**/dist
.turbo
.vite
.smithers
coverage
.DS_Store
# NEVER ship dev secrets into the image build context.
.githaiku-dev
**/.githaiku-dev
*.env
.env
.env.*
# The frontend is a separate deploy target: keep it out of the backend image
# context, but re-include its package.json so the pnpm --frozen-lockfile
# workspace validation in the Dockerfile can resolve all workspace manifests.
packages/frontend
!packages/frontend/package.json
